# pikaka-agent 项目结构与代码详解（零基础版）

> 这份文档假设你**刚接触 Agent 开发**，或者会一点 Python 但没写过 Agent。
> 我会从最基础的概念讲起，再逐行解释核心代码。目的是让你**真看懂**，而不是背下来。
> 源码位置：`D:\pikaka-agent`

---

# 第一部分：先搞懂这些基础概念

在讲代码之前，先花 10 分钟搞清楚几个词。这些词后面会反复出现。

### 1. 什么是 LLM（大语言模型）

LLM 就是 ChatGPT、Claude、GLM 背后那个「大脑」。你给它一段文字（叫 **prompt**，提示词），它返回一段文字。仅此而已——**它只会「文字进、文字出」**。

```python
输入:  "请用一句话介绍北京"
输出:  "北京是中国的首都。"
```

它不会自己上网、不会自己记东西、不会自己操作你的电脑。它只能「读文字 → 吐文字」。

### 2. 什么是 Agent（智能体）

既然 LLM 只会吐文字，那怎么让它「帮我订个会议」「帮我查个资料」？

**Agent 就是给 LLM 装上「手和脚」的一层外壳。** 核心思路是一个循环：

```
1. 把你的需求告诉 LLM
2. LLM 思考后说：「我需要调用『查日历』这个工具」
3. 程序真的去执行「查日历」，把结果拿回来
4. 把结果再喂给 LLM
5. LLM 看着结果，决定：继续调工具？还是直接回答你？
6. 重复 2-5，直到 LLM 说「我回答完了」
```

这个「思考 → 行动 → 看结果 → 再思考」的循环，就是 **Agent 的灵魂**。本项目 `loop/agent.py` 就是这个循环，只有 95 行。

> **通俗类比**：LLM 是一个「只会说话的聪明人」，Agent 是给他配了手、脚、笔记本的「办事员」。

### 3. 什么是「工具调用」（Function Calling / Tool Use）

LLM 怎么「调用工具」？这是现代大模型的一个能力：

你告诉 LLM：「你可以用这些工具，每个工具长这样（名字、参数说明）」。然后 LLM 需要时，会输出一段**特殊的文字**（不是给人看的，是给程序看的）：

```json
{ "tool": "create_event", "args": { "title": "网球", "time": "周六8点" } }
```

程序读到这段，就知道「哦，模型想调 `create_event` 这个工具，参数是这些」，然后**程序自己去执行**，再把执行结果以文字形式告诉 LLM。

> 关键点：**LLM 不真正执行工具，它只是「请求」执行。真正干活的是 Python 代码。** LLM 只是决策者。

### 4. Agent 的四大支柱

这个项目把 Agent 拆成四部分（作者称为「四大支柱」）：

| 支柱 | 通俗解释 | 生活类比 |
|---|---|---|
| **网关（Harness）** | 消息从哪进来、回复送到哪 | 前台接待：电话/微信/当面，都是「收发消息」 |
| **循环（Loop）** | 大脑怎么一步步干活 | 办事流程：「问需求→查资料→记录→汇报」 |
| **记忆（Memory）** | 怎么记住东西 | 笔记本：记事实、记发生的事、记办事方法 |
| **评测（Eval）** | 怎么证明「做对了」 | 考试：出题、批改、判断是否及格 |

### 5. 什么是 SQLite 和 FTS5

- **SQLite**：一个「零配置的数据库」，整个数据库就是一个文件（本项目是 `.pikaka/state.db`）。不需要装数据库服务器，Python 自带就能用。适合个人项目/单机程序。
- **FTS5**：SQLite 的一个**全文搜索**插件。普通 SQL 查询要精确匹配，FTS5 能做「关键词搜索」，就像搜索引擎那样，输入「Alex」能找到所有提到 Alex 的记录，还能按相关度排序。

> **类比**：普通数据库查询 = 在 Excel 里按「精确等于」筛选；FTS5 = 书后面的「索引页」，输入关键词快速找到相关段落。

### 6. 什么是评测（Eval）

Agent 会犯错（幻觉、做错事）。怎么知道它改没改好？**评测 = 自动测试**。

分两类（本项目严格区分，这是亮点）：
- **确定性评测**：有标准答案的，「做对了没」——是或否。比如「让 Agent 建一个会议，检查数据库里是不是真的多了这一行」。用 pytest 写，像单元测试。
- **LLM 裁判评测**：没有标准答案的，「回答得好不好」——打分。比如「这个回复有没有帮助」，让另一个 LLM 打分（0-100）。

> 把这两类混在一起，是 Agent 开发最常见的错误（用一个 LLM 去判断「建会议建对了没」既贵又不准）。

---

# 第二部分：这个项目到底是干什么的

**一句话**：一个跑在你电脑上的**个人助手**，但它的真正目的是**教学**——用最少的代码、不用任何框架，让你看清楚「一个 Agent 底层到底在干什么」。

它能真用：你可以跟它聊天、让它记东西、让它建日程。但它刻意把代码写得**极简、可读**（核心循环 95 行），目的就是让你读懂。

**技术栈（带解释）**：

| 技术 | 是什么 | 在项目里干嘛 |
|---|---|---|
| Python 3.11+ | 编程语言 | 全部代码 |
| Anthropic API | Claude 公司的模型接口 | 调用大模型的「标准协议」之一 |
| OpenAI 兼容层 | 另一套协议 | 让 GLM/DeepSeek 等国产模型也能接进来 |
| SQLite + FTS5 | 文件数据库 + 全文搜索 | 存记忆、聊天记录，做关键词检索 |
| pytest | Python 测试框架 | 确定性评测 |
| DeepEval | LLM 评测框架 | 裁判评测 |
| uv | 新一代 Python 包管理 | 装依赖、跑项目 |

**核心依赖只有 4 个**：`anthropic`、`openai`、`python-dotenv`、`rich`——作者刻意保持「核心极简」，其余能力（语音、Telegram 等）都做成可选扩展。

---

# 第三部分：整体架构（一张图看懂）

```
                    用户（在终端 / 网页输入一句话）
                              │
                    ┌─────────▼─────────┐
                    │  网关 Gateway       │  ← 只负责「收到消息、送回消息」
                    │  cli / 网页 / 语音  │
                    └─────────┬─────────┘
                              │ 把消息传给 app.py
                    ┌─────────▼─────────┐
                    │  工作记忆组装       │  ← 拼「系统提示词」
                    │  session.py        │     = 人设 + 记忆 + 历史 + 新消息
                    └─────────┬─────────┘
                              │
                    ┌─────────▼─────────┐
                    │  循环 The Loop      │  ← 思考→调工具→看结果→再思考
                    │  agent.py (95行)   │
                    └────┬───────┬──────┘
                         │       │ 调用
                  ┌──────▼──┐  ┌─▼──────────┐
                  │ 记忆系统  │  │ 工具 Tools  │  ← Agent 的「手」：查日历、
                  │ memory/  │  │ tools/     │    记笔记、搜网页…
                  │ (SQLite) │  └────────────┘
                  └─────────┘
                              │ 回复
                    ┌─────────▼─────────┐
                    │  可观测 + 评测      │  ← 记录过程、算花费、跑测试
                    │  ops/ + evals/    │
                    └───────────────────┘
```

**一次完整对话，发生了什么（用大白话）**：

1. 你输入「这周六上午9点帮我约网球」。
2. 网关收到，交给 `app.py` 的 `respond()` 方法。
3. `session.py` 拼好「系统提示词」：告诉 LLM 它是谁（人设）、现在几点、你之前说过什么、有没有相关的记忆。
4. `agent.py` 的循环开始：把提示词发给 LLM。
5. LLM 一看要建日程，返回「请调用 create_event 工具，参数是网球/周六9点」。
6. 循环执行 `create_event`，往数据库里插一条日程，把「已建好」告诉 LLM。
7. LLM 收到结果，说「已经帮你约好了周六9点的网球」。
8. 循环结束，回复给你。同时把这轮对话存进数据库，必要时整合成长期记忆。

---

# 第四部分：目录结构（每个文件一句话）

```
pikaka-agent/
├── pikaka/                        # 核心代码（整个 Agent 都在这里）
│   ├── __main__.py              # 入口：python -m pikaka
│   ├── app.py                   # ★「装配车间」：把所有零件拼成一个 Agent
│   ├── config.py                # 读配置（.env 文件里的模型、密钥等）
│   ├── db.py                    # ★数据库：建表、定义记忆存在哪
│   ├── loop/                    # 循环（Agent 的大脑运转方式）
│   │   ├── agent.py             #   ★核心循环，95行
│   │   └── models.py            #   接 11 个模型供应商
│   ├── runtime/
│   │   └── session.py           #   拼「系统提示词」（工作记忆）
│   ├── memory/                  # 记忆（三大类）
│   │   ├── __init__.py          #   Memory 总入口
│   │   ├── retrieval_gate.py    #   ★检索门：要不要翻记忆
│   │   ├── consolidation.py     #   整合：聊天→提炼成长期记忆
│   │   ├── semantic/            #   语义记忆（记「事实」）
│   │   ├── episodic/            #   情景记忆（记「发生过的事」）
│   │   └── procedural/          #   程序记忆（记「怎么做」= 技能）
│   ├── tools/                   # 工具（Agent 的手）
│   │   ├── registry.py          #   ★工具注册表：登记+执行工具
│   │   ├── calendar.py          #   建日程、查日程
│   │   ├── notes.py             #   记笔记
│   │   ├── search.py            #   搜网页
│   │   └── ...                  #   其他工具
│   ├── gateway/                 # 网关（消息入口）
│   │   ├── cli.py               #   终端聊天
│   │   └── runner.py            #   让 Telegram 等异步入口也能用
│   ├── graph/                   # 图工作流（可选的高级玩法）
│   └── ops/                     # 可观测（dashboard、记录、追踪）
├── evals/                       # 评测
│   ├── deterministic/           #   确定性（0/1）
│   └── judge/                   #   裁判（打分）
├── skills/                      # 内置技能（SKILL.md）
├── examples/                    # 教学示例
├── .env.example                 # 配置模板（你要复制成 .env）
└── pyproject.toml               # 项目定义 + 依赖清单
```

> 带 ★ 的是**最值得读的 6 个文件**，读代码建议按这个顺序：`app.py` → `agent.py` → `db.py` → `retrieval_gate.py` → `memory/__init__.py` → `tools/registry.py`。

---

# 第五部分：核心代码逐行详解 ★★★

> 这一部分是重点。我会贴关键代码，然后**逐段解释**。你不需要会写，只需要看懂「它在干什么、为什么这么写」。

## 5.1 配置 `config.py` —— Agent 的「设置面板」

```python
class Settings:
    provider: str = ...      # 用哪个模型供应商（glm / anthropic / openai…）
    api_key: str = ...       # 密钥
    model: str = ...         # 主模型（干活的）
    small_model: str = ...   # 小模型（做轻活：检索门、总结）
    home: Path = ...         # 数据存哪（默认 .pikaka/ 目录）
    max_iterations: int = 10 # 循环最多跑几轮（防死循环）
    max_tokens: int = 8192   # 单次最多吐多少字
    history_turns: int = 12  # 工作记忆保留最近几轮对话
```

**解释**：
- 这个类的作用是**把 `.env` 文件里的配置读进来，变成 Python 变量**，方便代码里用 `settings.model` 这种方式访问。
- 每个字段都有一个「默认值」（`default_factory`），意思是：如果你在 `.env` 里没写，就用默认值。
- 关键配置举例：
  - `max_iterations = 10`：Agent 循环最多跑 10 轮就强制停，防止它无限循环烧钱。
  - `history_turns = 12`：只把最近 12 轮对话放进提示词（这就是「滑动窗口」，防止对话太长把上下文撑爆、把成本撑高）。
  - `small_model`：**为什么要分「主模型」和「小模型」？** 主模型聪明但贵，用来认真干活；小模型便宜，用来做「要不要检索记忆」「总结聊天」这种简单判断。这是省钱的关键设计。

## 5.2 模型接入 `models.py` —— 怎么同时接 11 家模型

```python
PROVIDERS = {
    "anthropic": Provider("anthropic", "ANTHROPIC_API_KEY", None, "claude-sonnet-5", ...),
    "openai":    Provider("openai", "OPENAI_API_KEY", None, "gpt-5.5", ...),
    "deepseek":  Provider("openai", "DEEPSEEK_API_KEY", "https://api.deepseek.com", ...),
    "glm":       Provider("anthropic", "ZHIPU_API_KEY", "https://api.z.ai/api/anthropic", ...),
    # ... 共 11 个
}
```

**解释**：
- 每个模型供应商用一行 `Provider(...)` 描述。参数含义：
  1. 第一个：供应商名字
  2. 第二个：密钥在哪个环境变量里
  3. 第三个：API 地址（`None` 表示用官方默认地址）
  4. 第四、五个：默认的主模型、小模型名字
- 注意看 `"glm"` 那行的第一个参数是 `"anthropic"`——意思是 **GLM 走的是 Anthropic 协议**（因为智谱提供了 Anthropic 兼容接口）。而 `"deepseek"` 走的是 `"openai"` 协议。
- **核心思想**：不同公司的模型接口「方言」不同（就像不同国家的人说不同语言），这里用一个「适配器」统一翻译成同一种格式，让后面的循环代码**不用关心用的是哪家模型**。

具体怎么翻译的（关键函数）：

```python
def get_client(settings):
    provider = PROVIDERS[settings.provider]      # 找到对应配置
    api_key = os.getenv(provider.key_env)        # 读密钥
    if provider.kind == "anthropic":             # 走 Anthropic 协议的
        return anthropic.Anthropic(api_key=api_key, base_url=...)
    return OpenAICompatClient(api_key=..., ...)  # 走 OpenAI 协议的，包一层适配器
```

**解释**：
- `get_client` 返回一个「客户端对象」，它有一个统一的方法 `.messages.create(...)`（发消息给模型）。
- 不管背后是 Anthropic 还是 OpenAI 还是 GLM，**循环代码只认 `.messages.create()` 这一个方法**。
- `OpenAICompatClient` 就是那个「翻译官」（约 60 行），把 OpenAI 风格的接口翻译成 Anthropic 风格。

## 5.3 工作记忆 `session.py` —— 每次对话前「拼提示词」

```python
class Session:
    def build_system(self, user_message):
        # 1. 人设：告诉 LLM 它是谁
        parts = [load_soul(self.settings)]

        # 2. 当前时间：让 LLM 知道「现在几点」（否则"30分钟后"它算不出来）
        now = datetime.now()
        parts.append(f"Right now it is {now:%A, %Y-%m-%d %H:%M} ({now:%Z})")

        # 3. 模型自述：让 LLM 知道自己是哪个模型（有人会问"你是什么模型"）
        parts.append(f"Your model: you are running on '{settings.model}' ...")

        # 4. 相关记忆（经过检索门筛选，见 5.5）
        if self.memory is not None:
            retrieved = self.memory.gated_retrieve(user_message)  # 关键：先问"要不要检索"
            if retrieved:
                parts.append("Relevant memory:\n" + retrieved)

            # 5. 相关技能
            skills = self.memory.matching_skills(user_message)
            if skills:
                parts.append("Relevant skill instructions:\n" + skills)

        return "\n".join(parts)
```

**解释**：
- 这个方法的作用是**拼出「系统提示词」（system prompt）**——这是每次发消息给 LLM 之前，先告诉它的一堆「背景知识」。
- 拼的内容分 5 块，逐块看：
  1. **人设 SOUL.md**：一个可编辑的文本文件，写着「你是 Pikaka，一个简洁、温暖的助手…」。改这个文件就能改 Agent 的性格。
  2. **当前时间**：必须告诉 LLM 现在几点！否则你说「30 分钟后提醒我」，它不知道「现在」是几点。
  3. **模型自述**：让它知道自己是谁（这是很多人第一个会问的问题）。
  4. **相关记忆**：这里有个关键——**不是把全部记忆都塞进去**，而是先经过「检索门」判断要不要检索、检索什么（见 5.5）。
  5. **相关技能**：按关键词匹配到的技能说明。
- **工作记忆 = 用完就扔**。每次对话都重新拼一遍，拼完这次对话结束就丢弃。真正持久的东西存在数据库里（见 5.6）。

## 5.4 核心循环 `agent.py` —— 整个项目的灵魂（95 行）

这是最重要的一个文件。完整逻辑：

```python
def run_loop(client, model, system, messages, tools, max_iterations=10):
    for iteration in range(1, max_iterations + 1):   # 最多循环 max_iterations 轮

        # ---- 第 1 步：思考（reason）----
        response = client.messages.create(
            model=model, system=system, messages=messages, tools=tools.schemas(),
        )
        messages.append({"role": "assistant", "content": response.content})

        # 检查 LLM 有没有要求调用工具
        tool_uses = [b for b in response.content if b.type == "tool_use"]

        # ---- 第 2 步：如果没要调工具，说明它回答完了 → 结束 ----
        if not tool_uses:
            return "".join(b.text for b in response.content if b.type == "text")

        # ---- 第 3 步：行动（act）+ 观察（observe）----
        tool_results = []
        for call in tool_uses:
            output = tools.execute(call.name, call.input)   # 真正执行工具
            tool_results.append({"type": "tool_result", "content": output})
        messages.append({"role": "user", "content": tool_results})  # 结果喂回

    # ---- 循环跑满还没结束 → 硬停 ----
    return "(我达到迭代上限了...)"
```

**逐行解释（这是重点，慢慢看）**：

1. **`for iteration in range(1, max_iterations + 1)`**：这个 for 循环就是 Agent 的「命脉」。它最多转 `max_iterations`（默认 10）圈。为什么要有上限？**防止 LLM 陷入死循环**（比如它一直调同一个工具停不下来），10 圈到了强制结束。

2. **第 1 步 思考**：`client.messages.create(...)` 就是把「系统提示词 + 历史对话 + 工具清单」发给 LLM。LLM 会返回两样东西之一：
   - 一段给人看的文字（它觉得可以回答了）
   - 一个「工具调用请求」（它觉得需要先干点活）

3. **`messages.append(assistant 回复)`**：把 LLM 这轮的输出存进对话历史。这就是「观察」的一部分——让 LLM 下一轮「记得自己刚才说了什么」。

4. **`tool_uses = [b for b in ... if b.type == "tool_use"]`**：从 LLM 的回复里，挑出所有「工具调用请求」。（LLM 的回复是结构化的，有的块是文字 `text`，有的块是工具请求 `tool_use`。）

5. **`if not tool_uses: return 文本`**：如果 LLM 这轮没要求调工具，说明它已经回答完了，直接把文字返回给用户，**循环结束**。这是第一个「退出条件」。

6. **第 3 步 行动**：如果 LLM 要求调工具，就 `for call in tool_uses` 逐个执行。`tools.execute(call.name, call.input)` 真正去调用 Python 函数（比如真的往数据库插一条日程）。

7. **观察**：把工具的执行结果 `tool_results` 作为「用户消息」追加进历史，然后**回到 for 循环开头**，再让 LLM 看结果、继续决策。

8. **两个「退出条件」总结**（这是面试常问的）：
   - 条件 1：LLM 不再要调工具了 → 自然结束。
   - 条件 2：循环跑满 `max_iterations` 轮 → 硬停。

> **为什么说这是「所有 Agent 框架的本质」？** LangChain、LangGraph、AutoGPT 那些框架，内部干的就是这件事——只是加了很多层封装。这个项目把这个循环**赤裸裸地亮出来**，只有 95 行，让你一眼看穿。

## 5.5 检索门 `retrieval_gate.py` —— 项目的第一个「亮点」

先看问题：如果 Agent **每次对话都去数据库翻一遍记忆**，会怎样？
1. **慢**：每次回答前多一次搜索。
2. **更糟**：无关记忆会「带偏」回答。比如你问「2+2 等于几」，它却翻出「你周三有个会」，然后莫名提到开会的事。

解决思路：**先花一点点钱，让一个便宜的小模型判断「这条消息到底需不需要查记忆」**。

```python
GATE_PROMPT = """你是个人助手的检索门。
判断回答这个问题是否需要用到的记忆（关于人、项目、偏好的事实）。
只回复 JSON：{"retrieve": true/false, "query": "<搜索关键词>", "reason": "<5个字的原因>"}

用户消息: {message}"""

def should_retrieve(client, small_model, message):
    try:
        response = client.messages.create(
            model=small_model,           # 用便宜的小模型，省钱
            messages=[{"role": "user", "content": GATE_PROMPT.format(message=message)}],
        )
        text = "".join(b.text for b in response.content if b.type == "text")
        decision = json.loads(text)      # 解析出 {"retrieve": ..., "query": ...}
        return decision["retrieve"], decision["query"], decision["reason"]
    except Exception:
        return True, message, "gate failed"   # 出错就"宁可检索"（fail-open）
```

**逐行解释**：

1. **`GATE_PROMPT`**：这是一个模板提示词，让 LLM 只回答一个窄问题：「这条消息需要记忆吗？」。要求它**只返回 JSON**（不要废话），方便程序解析。

2. **`small_model`**：注意用的是**小模型**（不是主模型）。因为「判断要不要检索」是个简单任务，用贵的模型浪费钱。

3. **`json.loads(text)`**：把 LLM 返回的 JSON 字符串解析成 Python 字典，拿到 `retrieve`（要不要检索）、`query`（搜什么关键词）、`reason`（为什么）。

4. **`except: return True`（fail-open）**：这是关键设计！如果门自己出错了（比如返回的不是合法 JSON），就**默认「检索」**。宁可多查一次（慢一点），也不漏掉重要记忆。这叫 **fail-open（失败时打开）**，对比 fail-closed（失败时关闭/不检索，可能丢记忆）。

**效果**：

```
你 > 2+2 等于几？
  gate · skip（跳过检索，纯数学）
你 > 我什么时候和 Alex 开会？
  gate · retrieve（检索，因为提到了"和 Alex 开会"）
```

> **面试加分点**：这是一个非常漂亮的工程权衡——用极低的成本（小模型几百 token），换来了「只在需要时检索」，同时保证「出错也不丢记忆」。它体现了「小模型做窄决策」这个通用模式。

## 5.6 数据库 `db.py` —— 记忆存在哪、怎么存

```sql
-- 语义记忆：关于你、你朋友、你项目的「持久事实」
CREATE TABLE facts (
    id INTEGER PRIMARY KEY,
    subject TEXT,      -- 事实是关于谁的，比如 'alex'
    content TEXT,      -- 事实本身，比如 '喜欢早上开会'
    source TEXT,       -- 来源：'user'（你直接说的）或 'consolidation'（整合提炼的）
    created_at TEXT
);

-- FTS5 全文索引：让上面这张表可以"关键词搜索"
CREATE VIRTUAL TABLE facts_fts USING fts5(subject, content, content=facts);

-- 触发器：往 facts 表插入数据时，自动更新全文索引
CREATE TRIGGER facts_ai AFTER INSERT ON facts BEGIN
    INSERT INTO facts_fts(...) VALUES (...);
END;

-- 情景记忆：带日期的「发生过的事」
CREATE TABLE episodes (id, happened_at, summary, ...);

-- 聊天记录：原始对话（整合从这里读）
CREATE TABLE chat_log (id, role, content, consolidated, session_id, ...);
```

**逐段解释**：

1. **`facts` 表**：这是「语义记忆」——记**事实**。比如「Alex 喜欢早上开会」。字段 `source` 很有意思：能区分这条事实是**你直接告诉它的**（user）还是**它自己从聊天里提炼出来的**（consolidation）。

2. **`facts_fts`（FTS5 虚拟表）**：这是「全文搜索索引」。它让 `SELECT ... WHERE facts_fts MATCH 'alex'` 这种「模糊关键词搜索」成为可能（普通 SQL 只能精确匹配）。

3. **触发器 `facts_ai`**：为什么需要触发器？因为 `facts` 表（存数据）和 `facts_fts` 表（存索引）是**两张表**，需要保持同步。触发器的作用是：**每当往 `facts` 插一条数据，自动往 `facts_fts` 也插一条**，你不用手动管同步。

4. **`episodes` 表**：这是「情景记忆」——记**带时间的事**。比如「2026-07-10：和 Alex 策划了 Acme 演示」。和 `facts` 的区别：`facts` 是「现在仍然成立的事实」，`episodes` 是「某天发生的事」。

5. **`chat_log` 表**：这是「原始聊天记录」——每一轮对话都原样存下来。它是「整合」（consolidation，见 5.7）的原材料。

> **为什么用 SQLite 而不用向量数据库？** 面试常问。答案：单用户的事实量级（几百条）下，**关键词搜索（FTS5 的 BM25 算法）完全够用**，而且全本地、能直接打开看。向量数据库（如 pgvector）是给海量数据做「语义相似」搜索用的，这里杀鸡不用牛刀。项目也预留了升级路径（`PIKAKA_SEMANTIC_STORE=supabase`）。

### 5.6.1 补充：中文搜索的坑（一个非常加分的技术细节）

`semantic/store.py` 里有一段处理中文搜索的代码：

```python
_UNSEGMENTED = re.compile("[一-龥ぁ-んァ-ン...]")  # 中日韩文字

def _fts_query(text):
    words = re.findall(r"[^\W_]{2,}", text.lower())
    return " OR ".join(
        f"{w}*" if _UNSEGMENTED.search(w) else w   # 中文词加 * 前缀匹配
        for w in words
    )
```

**解释（这是亮点，值得在面试讲）**：
- FTS5 默认的「分词器」（tokenizer）是给英文设计的——它按空格和标点分词。英文「I like Alex」能正确分成 `I / like / Alex`。
- 但中文**没有空格**！「阿历克斯喜欢游泳」会被当成**一整个词**（因为分词器看不懂中文，不切分）。这样你搜「阿历克斯」就搜不到（因为索引里存的是「阿历克斯喜欢游泳」这一整串）。
- 解决方法：检测到中文，就把搜索词变成**前缀匹配**（`阿历克斯*`），让它匹配「阿历克斯喜欢游泳」这个整串的前缀。
- 这个细节说明作者**真正处理过中文用户**，而不是嘴上说说「支持多语言」。面试时讲出来，非常加分。

## 5.7 记忆系统 `memory/` —— 三大类记忆 + 整合

### 三大类记忆（门面类 `Memory` 统一管理）

```python
class Memory:
    def __init__(self, conn, settings, client):
        self.facts = SqliteFactStore(conn)      # 语义记忆：记事实
        self.episodes = SqliteEpisodeStore(conn) # 情景记忆：记事件
        self.skills = SkillLoader(dirs)          # 程序记忆：记技能

    def gated_retrieve(self, message):
        # 先问检索门"要不要检索"
        retrieve, query, reason = retrieval_gate.should_retrieve(...)
        if not retrieve:
            return ""                             # 不检索，返回空
        # 检索事实 + 情景，拼起来返回
        found = self.facts.search(query, top_k=4)
        found += self.episodes.search(query, top_k=3)
        return "\n".join(found)
```

**解释**：`Memory` 是个「门面」（facade），把三类记忆封装在**一个统一接口**后面。外面只需要调 `memory.gated_retrieve(消息)`，不用关心内部有三类记忆。

### 整合 `consolidation.py` —— 从聊天里「提炼」长期记忆

```python
def consolidate_if_due(conn, client, small_model, every_n, facts, episodes):
    # 1. 读还没整合的聊天记录
    rows = conn.execute("SELECT * FROM chat_log WHERE consolidated = 0").fetchall()
    if len(rows) < every_n * 2:     # 攒够 N 轮才整合（每轮=2条：用户+助手）
        return 0

    # 2. 让小模型读聊天，提炼出事实和事件
    response = client.messages.create(
        model=small_model,
        messages=[{"role": "user", "content": SUMMARIZER_PROMPT.format(log=log)}],
    )
    distilled = json.loads(response)   # {"facts": [...], "episode": "..."}

    # 3. 写进记忆表
    for fact in distilled["facts"]:
        facts.add(fact["subject"], fact["content"], source="consolidation")
    if distilled.get("episode"):
        episodes.add(distilled["episode"], happened_at=today)

    # 4. 标记这些聊天"已整合"
    conn.execute("UPDATE chat_log SET consolidated = 1 WHERE ...")
```

**解释**：
- **为什么要整合？** 聊天记录是「原始素材」，太长太杂，不适合直接当长期记忆。整合就是**用 LLM 把聊天压缩成几条精炼的事实**。
- **为什么「每 N 轮才整合一次」而不是每轮都整合？** 每轮都调用 LLM 总结太浪费。攒够 N 轮（默认 6）一次性总结，既省钱，又让 LLM 有足够上下文提炼出真正值得记的东西。
- 例：聊天里你随口说「我下周要出差去上海」，整合后变成一条事实 `[出差] 下周去上海`。
- **失败保护**：如果整合出错（LLM 返回非 JSON），就 `return 0`，聊天记录保持「未整合」状态，下次再试，**不丢数据**。

## 5.8 工具 `tools/registry.py` —— Agent 的「手」

```python
@dataclass
class Tool:
    name: str              # 工具名，比如 "create_event"
    description: str       # 给 LLM 看的说明（告诉它这工具干嘛的）
    input_schema: dict     # 参数定义（JSON Schema 格式）
    fn: Callable           # 真正干活的 Python 函数

class ToolRegistry:
    def register(self, tool): ...    # 登记工具

    def schemas(self): ...           # 把所有工具的定义发给 LLM

    def execute(self, name, args):
        tool = self._tools.get(name)
        if tool is None:
            return f"Error: unknown tool '{name}'"
        try:
            return tool.fn(**args)                     # 真正执行
        except Exception as exc:
            return f"Error running {name}: {exc}"       # 出错返回文本，不崩溃
```

**解释**：
- 一个工具 = 三样东西：**名字 + 说明 + 干活函数**。`schemas()` 把「名字+说明+参数定义」发给 LLM（让 LLM 知道有哪些工具能用）；`execute()` 在 LLM 请求时真正执行。
- **关键设计：工具出错时返回文本，而不是让程序崩溃。** 比如 `create_event` 执行失败，返回字符串 `"Error running create_event: ..."`，这个错误文本会作为「工具结果」喂回给 LLM，LLM 看到错误后可以**换个方式重试**。如果直接抛异常崩溃，整个循环就断了。

> **类比**：工具就是 Agent 的「手」。LLM 是「大脑」，它只会说「我想做 X」；`ToolRegistry` 是「手臂」，真正去执行 X。

## 5.9 装配 `app.py` —— 把所有零件拼起来

```python
class Pikaka:
    def __init__(self, settings=None, client=None, conn=None):
        self.settings = settings or load_settings()      # 1. 读配置
        self.conn = conn or connect(self.settings.home)  # 2. 连数据库
        self.client = client or get_client(self.settings)# 3. 连模型
        self.memory = Memory(self.conn, self.settings, self.client)  # 4. 记忆
        self.tools = build_registry(...)                 # 5. 工具
        self.session = Session(self.settings, memory=self.memory)   # 6. 会话
        self.tracer = Tracer(self.settings)              # 7. 记录

    def respond(self, user_message):
        system = self.session.build_system(user_message)   # 拼提示词
        result = run_loop(self.client, self.settings.model,
                          system, messages, self.tools)    # 跑循环
        self.session.add_exchange(...)                     # 存这轮对话
        self.memory.maybe_consolidate()                    # 必要时整合
        return result.reply
```

**解释**：
- `app.py` 是「**装配车间**」：把 config、db、model、memory、tools、session、tracer 这 7 个零件，按顺序组装成一个能用的 `Pikaka` 对象。
- `respond()` 方法是「**总调度**」：一次对话 = 拼提示词 → 跑循环 → 存记录 → 必要时整合。整个项目的「骨架」就是这个方法。
- **依赖注入**（面试亮点）：注意 `client` 和 `conn` 可以作为参数传进来。好处是：**测试时可以传「假的模型」**（不用真花钱调 LLM），**网页版可以传「跨线程的数据库连接」**。这是让代码「可测试」的关键。

> **读代码建议**：如果你只看一个文件，就看 `app.py`。它是「装配图」，看完它你就知道整个项目是怎么串起来的。

## 5.10 网关 `gateway/runner.py` —— 让 Telegram 等入口也能用

```python
class GatewayAgentRunner:
    def __init__(self, ...):
        self._executor = ThreadPoolExecutor(max_workers=1)  # 单线程池

    async def respond(self, text):
        return await loop.run_in_executor(self._executor, self._respond, text)
```

**解释**：
- 为什么需要这个？因为 Telegram、Discord 这些入口是**异步**的（Python 的 async/await），而 Pikaka 的循环是**同步**的，而且独占一个 SQLite 连接。
- 解决：用**单线程的线程池**（`max_workers=1`）把同步的 Pikaka 包起来。这样每个网关有一个「稳定的工作线程」，事件循环保持响应，回合按顺序执行，SQLite 在同一线程安全使用。
- 还做了**密钥脱敏**：出错信息里如果包含密钥（`*_API_KEY`、`*_TOKEN` 等），自动替换成 `***`，防止密钥泄露到日志里。

---

# 第六部分：8 个关键技术设计（为什么这么写）

> 这一节是「面试能讲的亮点」，每一条都对应一个「为什么」。

### 亮点 1：检索门（Retrieval Gate）—— 用小模型省钱 + 抗偏置
**问题**：每轮都检索记忆 → 慢 + 无关记忆带偏答案。
**方案**：便宜小模型先判断「要不要检索」，失败时 fail-open（宁可检索）。
**价值**：体现「用最小成本做正确决策」的工程判断力。

### 亮点 2：两类评测分离 —— 确定性 vs 裁判
**问题**：把「做对了没」（客观）和「好不好」（主观）混为一谈。
**方案**：确定性用 pytest（0/1），裁判用 DeepEval（打分），`make gate` 一起跑当发布门。
**价值**：这是 Agent 开发最常见的坑，你能讲清楚说明你理解了评测的本质。

### 亮点 3：SQLite FTS5 + 中文分词处理
**问题**：向量库太重，FTS5 默认不切中文。
**方案**：FTS5 关键词检索 + 对中日韩文字做前缀匹配。
**价值**：体现「按规模选技术」+「真处理过多语言」的细节。

### 亮点 4：多供应商适配器
**问题**：11 家模型接口不统一。
**方案**：统一成一种格式，OpenAI 系用 ~60 行适配器翻译。
**价值**：体现「抽象能力」——循环不感知供应商。

### 亮点 5：图工作流 fail-open 到循环
**问题**：复杂任务需要「结构化流程」，但不想破坏简单循环。
**方案**：可选的三分类图（小模型分类→快速回复 or 完整循环），任何失败都回退到普通循环。
**价值**：体现「渐进式增强，不破坏核心」。

### 亮点 6：依赖注入 + 可测试性
**方案**：`Pikaka(client=..., conn=...)` 让测试能注入假模型。
**价值**：体现「可测试性」意识——这是从「会写」到「工程化」的分水岭。

### 亮点 7：可观测性 —— trace 永远开、花费永久记账
**方案**：每轮写 JSONL trace（零配置）；每次 LLM 调用记 token 到只增不改的账本。
**价值**：体现「生产级 Agent 要能追踪、能算账」。

### 亮点 8：渐进式披露（技能加载）
**方案**：技能的标题常扫（便宜）、正文匹配才加载、引用文件按需读。
**价值**：体现「控制上下文成本」的意识。

---

# 第七部分：术语表（速查）

| 术语 | 一句话解释 |
|---|---|
| **LLM** | 大语言模型，只会「文字进、文字出」的大脑 |
| **Agent** | 给 LLM 装上工具、记忆、循环的外壳 |
| **Prompt** | 发给 LLM 的输入文字 |
| **System Prompt** | 系统提示词，告诉 LLM 它的身份和规则 |
| **Tool / Function Calling** | LLM 请求程序执行某个函数的能力 |
| **Loop** | Agent 的「思考→行动→观察」循环 |
| **Retrieval Gate** | 检索门：判断「要不要查记忆」的小模型 |
| **Semantic Memory** | 语义记忆：记「事实」 |
| **Episodic Memory** | 情景记忆：记「发生过的事」 |
| **Procedural Memory** | 程序记忆：记「怎么做」（技能） |
| **Consolidation** | 整合：把聊天提炼成长期记忆 |
| **SQLite** | 零配置的文件数据库 |
| **FTS5** | SQLite 的全文搜索插件 |
| **BM25** | 一种关键词相关度排序算法 |
| **fail-open** | 失败时选择「更安全/更宽松」的行为 |
| **依赖注入** | 把依赖（如模型）作为参数传入，便于替换和测试 |
| **Eval** | 评测，自动测试 Agent 的表现 |
| **确定性评测** | 有标准答案的测试（0/1） |
| **LLM 裁判评测** | 用 LLM 打分的评测 |
| **token** | 模型处理文本的最小单位，约等于 3/4 个英文单词 |
| **editable install** | `pip install -e`，装成「可编辑」模式，改代码即时生效 |

---

# 第八部分：简历怎么写 + 面试怎么答

### 简历 bullet（可直接用，中文版）

> **pikaka-agent · 本地优先个人助手 Agent**（Python / SQLite / Anthropic API）
>
> - 研读并部署了一个不依赖框架的 Agent 系统，核心循环仅 ~95 行纯 Python，覆盖「网关 → 工作记忆 → 循环 → 记忆 → 评测」完整链路
> - 理解并实现 **Retrieval Gate**（低成本小模型 judge 决定是否检索记忆，fail-open 兜底），在保证准确性的同时降低 token 成本与无关记忆带来的答案偏置
> - 使用 **SQLite + FTS5** 构建三支柱记忆系统（语义/情景/程序），并处理了 FTS5 对中日韩文字不切词的问题
> - 通过 ~60 行适配器统一接入 11 个 LLM 供应商（Anthropic / OpenAI 两种协议），做到「换供应商不改循环」
> - 建立**确定性评测（pytest）与 LLM 裁判评测（DeepEval）分离**的评测体系，作为发布门
> - 实现全链路可观测：JSONL trace 常开 + token 花费永久记账 + Web dashboard 可视化

### 面试高频问答

**Q1：这个项目是做什么的？**
一个本地优先的个人助手 Agent，教学导向。一句话 + 四大支柱（网关/循环/记忆/评测）。强调「不用框架、核心循环 95 行、代码可读」。

**Q2：Agent 循环怎么实现的？**
`while` 循环：思考（调 LLM）→ 行动（执行工具）→ 观察（结果喂回）。两个退出条件：模型不再要工具（自然结束）/ 达到 max_iterations（硬停防死循环）。和 LangGraph 的区别：这是无框架最简形态，LangGraph 是显式图编排。

**Q3：为什么用 SQLite + FTS5，不用向量数据库？**
单用户事实量级下，关键词 BM25 够用、全本地、索引可读。想用向量可切 `PIKAKA_SEMANTIC_STORE=supabase`（pgvector），接口不变。核心是「按规模选技术」。

**Q4：检索门是什么？解决什么问题？**
每轮都查记忆 → 慢 + 无关记忆带偏答案。用小模型先判断「要不要检索」，fail-open 兜底。这是「小模型做窄决策」的 judge 模式。

**Q5：怎么保证 Agent 不胡说 / 评测怎么做？**
确定性评测（pytest，断言数据库行）测「做对了没」；LLM 裁判（DeepEval 打分）测「好不好」。两类严格分离。

**Q6：你从这个项目学到什么？（开放题，重点准备）**
从「用 Agent」到「看懂 Agent」——理解了循环、工作记忆、检索门、记忆持久化、评测这些概念是怎么落成代码的；以及「可读性、可观测性、可测试性」在生产 Agent 里的价值。

---

> **最后的学习建议**：
> 1. 先跑起来（`uv run pikaka dashboard`），边用边看 dashboard 里 loop、memory 怎么动。
> 2. 然后按这个顺序读代码：`app.py` → `agent.py` → `db.py` → `retrieval_gate.py` → `memory/__init__.py` → `tools/registry.py`。
> 3. 每读一个文件，就对着第五部分的逐行解释看一遍。
> 4. 面试前，重点练 Q1、Q2、Q4、Q6 这四个问题。
