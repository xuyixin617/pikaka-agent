# pikaka-agent 面试实习全梳理

> 这份文档是给「投实习简历 + 面试」用的完整准备材料。它不重复《项目结构与代码详解》里的逐行代码讲解，而是站在**「你怎么把这件事讲清楚」**的角度：项目是什么、怎么搭的、用了什么技术、前后端怎么分工、怎么证明它能用、你学到了什么、能改进什么、面试会问什么。
>
> 所有技术事实都已对着源码核对过，可以放心照着讲。

---

## 0. 这份文档怎么用

- 面试前通读一遍，重点背 **第 8 节** 的高频问答和 **第 9 节** 的简历 bullet。
- 自我介绍用 **第 1 节**（30 秒版）＋ **第 2 节**（1 分钟版）。
- 被问到「你在这个项目里做了什么、踩了什么坑」时，翻 **第 3 节**。
- 被问到「你怎么证明它好用」时，翻 **第 5 节**。
- 被问到「还有什么不足」时，翻 **第 7 节**——这一节最能体现你的批判性思维。

---

## 1. 项目是什么（30 秒自我介绍版）

> 「我部署并研读了一个叫 **pikaka-agent** 的开源项目——一个跑在本地的个人助手 Agent。它最大的特点是不用任何 Agent 框架，用**不到 100 行的纯 Python** 把 Agent 最核心的四样东西——**网关、循环、记忆、评测**——全部实现出来，代码一个下午就能读完。我通过它搞清楚了 ChatGPT 这类产品底层到底在干什么：LLM 是怎么一步步调用工具的、记忆是怎么存和查的、怎么用评测证明它做对了。」

**一句话定位**：一个**本地优先、教学导向**的个人助手 Agent，把「Agent 的骨架」用最少的代码、零框架地展示出来。

**它和 ChatGPT / Claude Desktop 的区别**：那些是「你用的产品」，这个是「你能读懂、能改、能拥有代码的骨架」。读懂了它，你就读懂了产品底层的循环、记忆、评测。

---

## 2. 项目全景：架构 + 技术栈 + 工具

### 2.1 架构：四个支柱，一张图

pikaka-agent 把 Agent 拆成四大支柱（这是面试讲架构的主线）：

| 支柱 | 英文 | 通俗解释 | 对应代码 |
|---|---|---|---|
| 网关 | Harness | 消息从哪进、回复送到哪 | `pikaka/gateway/` |
| 循环 | Loop | 大脑怎么一步步干活 | `pikaka/loop/agent.py`（~95 行） |
| 记忆 | Memory | 怎么记住东西（三类） | `pikaka/memory/` + `pikaka/db.py` |
| 评测 | Eval/LLM-Ops | 怎么证明做对了 | `evals/` + `pikaka/ops/` |

**一次对话的数据流**（面试画图用）：

```
用户输入 → 网关(CLI/网页/语音/Telegram)
         → 拼工作记忆(session.py: 人设+时间+相关记忆+历史)
         → 循环(agent.py: 思考→调工具→看结果→再思考)
              ├─ 要查记忆时 → 检索门判断(小模型) → SQLite/FTS5 检索
              └─ 要干活时 → 工具(建日程/记笔记/搜网页)
         → 回复用户
         → 同时：存聊天记录 + 必要时整合成长期记忆 + 记 trace + 记花费
```

### 2.2 技术栈清单（分层，面试直接念）

| 层 | 技术 | 说明 |
|---|---|---|
| 语言 | Python 3.11+ | 全部代码 |
| 包管理 | **uv** | 新一代 Python 包管理器（比 pip 快、自带 venv） |
| 核心依赖（仅 4 个） | `anthropic` / `openai` / `python-dotenv` / `rich` | 两个是模型 SDK，dotenv 读配置，rich 做终端美化 |
| 模型接入 | 11 家供应商 | Anthropic / OpenAI / Gemini / DeepSeek / MiniMax / Kimi / GLM / OpenRouter 等，走两种协议 |
| 数据库 | **SQLite + FTS5** | 整个记忆就是一个 `.pikaka/state.db` 文件，FTS5 做全文关键词检索 |
| 后端服务器 | **Python 标准库 `http.server`** | dashboard 网页服务，零第三方依赖 |
| 前端 | **纯静态 HTML/CSS/JS** | 无框架、无构建步骤 |
| 确定性评测 | **pytest** | 「做对了没」→ 0/1 |
| 裁判评测 | **DeepEval** | 「好不好」→ 打分 |
| 深度追踪 | **Phoenix (arize) + OpenTelemetry** | 可选的 span 级追踪（默认关闭） |
| 可选网关 | python-telegram-bot / discord.py / faster-whisper(语音) 等 | 每个都是 opt-in 的 extra |

**关键一句话**：**核心只有 4 个第三方依赖 + Python 标准库**，其余所有能力（语音、Telegram、Google 日历、向量库、深度追踪）都是「按需安装的可选 extra」，且**不改变核心循环**。

### 2.3 用到的工具汇总

- **开发/运行**：uv（venv + 安装）、git、VS Code
- **模型**：GLM（本项目实际接入的是智谱 GLM-5.2，走 Anthropic 兼容端点）
- **测试**：pytest、DeepEval
- **可观测**：自研 JSONL trace + Phoenix（可选）+ 自研花费账本（usage.jsonl）

---

## 3. 我是怎么搭建的（完整步骤 + 踩坑）

### 3.1 标准搭建步骤

```bash
# 1. 克隆源码（读代码用，而不是 pip install 成品）
git clone https://github.com/ShenSeanChen/waku-agent
cd pikaka-agent

# 2. 建虚拟环境 + 以"可编辑"方式安装（这样改代码即时生效）
uv venv
uv pip install -e .

# 3. 配置模型供应商
cp .env.example .env      # 复制模板
#    编辑 .env：填 PIKAKA_PROVIDER=glm、ZHIPU_API_KEY=...、ZHIPU_BASE_URL=...

# 4. 运行
uv run pikaka               # 终端聊天
uv run pikaka dashboard     # 网页驾驶舱 → localhost:7777
```

`uv run pikaka ...` **不需要先 activate 虚拟环境**，uv 会自动找对 Python。

### 3.2 我踩过的最关键的坑（面试必讲，这是你的真实经历）

**坑：Windows 中文用户名导致 Python 崩溃。**

我的 Windows 用户名是中文（`C:\Users\徐奕欣`），系统编码是 GBK。当项目用 `uv pip install -e .`（可编辑安装）时，会在 site-packages 里生成一个 `.pth` 文件，里面记录着项目的**绝对路径**。Python 每次启动读这个 `.pth` 文件时，用 GBK 去解码含中文的路径，直接报 `UnicodeDecodeError: 'gbk' codec can't decode byte 0xa5`，导致整个 Python 崩溃、连 site 模块都导不进来。

我试了 `-X utf8` 和 `PYTHONUTF8=1` 都**没用**（这个 Python 版本读 `.pth` 的方式绕不过 GBK）。最终解法是：**把项目移到无中文的路径（`D:\pikaka-agent`）**，问题解决。

**这个坑的价值**：它体现的不是「我会 Google」，而是「我能定位到一个很底层的编码/路径问题，并找到真正的根因」。面试讲这个，比背八股有说服力得多。

### 3.3 其它小坑（一笔带过即可）

- Windows 控制台 GBK 显示 emoji/中文会乱码，但不影响功能（`PYTHONIOENCODING=utf-8` 可缓解）。
- 智谱的 Anthropic 兼容端点只支持 glm-4.5 以上的新模型，不支持老的 glm-4（老模型只在 OpenAI 端点）——配置时要选对模型名。

---

## 4. 前端后端分别怎么搭的（如实说明：这是单体，不是前后端分离）

面试如果问「你这个项目前端后端怎么分的」，**一定要如实说**，不要假装是 React + Spring 那套。如实说反而加分——说明你理解「不是所有项目都需要前后端分离」。

### 4.1 后端（真正的「大脑」，全部在 `pikaka/` 包）

后端不是「一个 web 服务」，而是**一个可复用的 Python 核心**，分几块：

1. **装配层** `app.py`：`Pikaka` 类把配置、数据库、模型、记忆、工具、会话、追踪器组装起来，`respond()` 串起一次完整对话。
2. **循环** `loop/agent.py`：`run_loop()`，~95 行纯 Python，`while` 循环 + 两个退出条件。
3. **记忆** `memory/` + `db.py`：SQLite 五张表 + FTS5 全文索引 + 检索门 + 整合。
4. **工具** `tools/registry.py`：`ToolRegistry` 统一登记和执行工具，出错返回文本不崩溃。
5. **模型接入** `loop/models.py`：11 家供应商，用 ~60 行适配器统一成一种调用格式。

**后端的技术选型逻辑**：核心只用 stdlib + 两个模型 SDK，刻意不引入 FastAPI/Django 这类 web 框架——因为「Agent 循环」本身不依赖任何 web 框架。

### 4.2 前端（dashboard 的 UI，极简到极致）

dashboard 的「前端」是**纯静态文件**，没有任何框架和构建步骤：

```
pikaka/ops/static/
├── index.html   # 页面结构
├── style.css    # 样式
└── app.js       # 交互逻辑（调 /api/... 接口、渲染数据）
```

- **无 React / Vue / 打包工具**：直接手写 HTML/CSS/JS，浏览器直接加载。
- **服务器是 Python 标准库 `http.server`**（`ThreadingHTTPServer`），不是 FastAPI/Flask。
- **零新增依赖**：dashboard 功能在核心 4 个依赖之外，不装任何东西。
- **只绑定 `127.0.0.1`**：只能本机访问，数据不出电脑，符合「local-first」。
- 前端页面有 8 个 tab（Overview / Gateway / Loop / Graph / Memory / Tools / Data / Ops），每个 tab 对应一个支柱，实时读取后端进程写的文件（traces/*.jsonl、state.db、usage.jsonl）。

**为什么这么设计**：这是教学仓库，作者想让「看 dashboard」这件事也零门槛——打开浏览器就能看到 Agent 的每个环节怎么动，而不是先要会 React。

### 4.3 网关：一个大脑，多个入口

「网关」是第三块。同一个 `Pikaka` 核心，可以接多个入口：

| 入口 | 命令 | 说明 |
|---|---|---|
| 终端 | `pikaka` | 命令行聊天 |
| 网页 | `pikaka dashboard` | 浏览器驾驶舱 |
| 语音 | `pikaka voice` | 说话交互（需 `[voice]` extra） |
| Telegram | `pikaka telegram` | 手机发消息给电脑 |
| Discord | `pikaka discord` | Discord 机器人 |
| WhatsApp | `pikaka whatsapp` | 需公网 URL |

关键设计：**这些入口共用一个 `Pikaka` 实例**（通过 `gateway/runner.py` 的线程池把同步核心包装成异步接口），所以「你在终端聊一句、在网页聊一句」，Agent 是同一个大脑、同一份记忆。

---

## 5. 怎么测试这个项目的可用性和可靠度

这一节是「怎么证明它好用」，面试常问。pikaka-agent 的评测体系是它的**第二大亮点**（第一大是检索门）。

### 5.1 核心思想：把两种评测严格分开

| | 确定性评测 | LLM 裁判评测 |
|---|---|---|
| 回答的问题 | 「做对了没？」（客观） | 「好不好？」（主观） |
| 工具 | pytest | DeepEval |
| 结果 | 0 / 1（通过 / 失败） | 0-100 分（带阈值） |
| 谁评判 | 代码断言，**不用模型** | 另一个 LLM 打分 |
| 例 | 「建日历事件后，数据库里是不是真的多了这一行」 | 「这个回复有没有帮助」 |

**为什么分开是亮点**：把「做对了没」和「好不好」混在一起，是 Agent 评测最常见的错误——用 LLM 去判断「日历建对了没」既贵又不准。能讲清这个，说明你真正理解了评测的本质。

### 5.2 三个命令

```bash
make eval          # 确定性：跑全部 pytest，必须 100% 通过
make eval-judge    # 裁判：DeepEval 打分，需要 API key
make gate          # 发布门：两类都跑，确定性 100% + 裁判过阈值才算通过
```

### 5.3 确定性测试覆盖了什么（举真实例子）

`evals/deterministic/` 下有 **60 多个测试文件**，覆盖了几乎每个模块。面试可以举几个具体的：

- `test_retrieval_gate.py` —— 检索门「要不要检索」的判断是否正确
- `test_working_memory.py` —— 工作记忆是否正确包含当前时间（这是修过一个真实 bug 后加的回归测试）
- `test_consolidation.py` —— 记忆整合是否把聊天正确提炼成事实
- `test_providers.py` / `test_pinned_models.py` —— 11 家供应商配置是否正确
- `test_dashboard_routes.py` —— dashboard 的 API 路由是否正常
- `test_html_escaping.py` —— 前端是否有 XSS 隐患（HTML 转义）

**最有说服力的一个例子**（README 里写的真实 bug 流程）：
> 早期版本 Agent 不知道「当前时间」，你让它「30 分钟后提醒」，它反而先问你现在几点。修复方式：在 `session.py` 里把当前时间塞进系统提示词；再补一个 `test_working_memory.py` 测试锁死这个行为，让它永远不再犯。

这个「**抓到 bug → 修 → 加回归测试**」的闭环，就是你要在面试里展示的工程素养。

### 5.4 可观测性（怎么「看得见」它干得对不对）

评测是「事后验证」，可观测是「实时看着它干活」：

- **Trace 永远开着**：每一轮对话都往 `.pikaka/traces/<日期>.jsonl` 追加一行可读记录，顺序是 `turn_start → gate → llm → tool → llm → turn_end`。零配置。
- **花费永久记账**：每次 LLM 调用的 token 都追加到 `.pikaka/usage.jsonl`——一个**只增不改**的账本。所以 dashboard 显示的成本是你的真实累计花费，不是单次会话的估算。
- **dashboard 实时可视化**：8 个 tab，能实时看到「这轮检索门跳过了没、循环跑了几次、调了哪些工具、花了多少钱」。

### 5.5 我自己的验证过程（面试讲「你怎么确认它真的能用」）

1. 跑通端到端对话：GLM 正常返回，能建日历、能记笔记。
2. 验证记忆持久化：让它「记住 Alex 喜欢早上开会」，退出重启，再问「Alex 开会偏好」，它能答上来（证明记忆真的存进了 SQLite）。
3. 验证检索门：问「2+2 等于几」（应该 skip，不查记忆）vs「我什么时候和 Alex 开会」（应该 retrieve），对比两个决策。
4. 打开 dashboard，看一次对话完整地流经 gate → loop → tool → memory。
5. 跑 `make eval` 确认确定性测试全绿。

---

## 6. 我从这个项目里学到了什么（能力成长，面试必答）

这是「开放题」，建议分三层讲：

### 6.1 技术层面（具体技能）

- **理解了 Agent 的本质**：`while` 循环 + 工具调用 + 记忆，LangChain/LangGraph 那些框架内部就是这回事。从「用 Agent」到「看懂 Agent」。
- **记忆系统设计**：语义/情景/程序三类记忆怎么区分、怎么存（SQLite + FTS5）、怎么查（检索门 + 关键词搜索）、怎么整理（整合）。
- **多模型适配**：不同供应商的接口「方言」不同，怎么用一个适配器统一。
- **评测体系**：确定性 vs 裁判的分离，为什么这么重要。
- **可观测性**：trace、花费记账对一个生产 Agent 有多重要。

### 6.2 工程层面（软技能）

- **可读性优先**：核心循环 95 行、无框架——「代码写得让人读得懂」本身是工程能力。
- **可测试性**：依赖注入（`client`/`conn` 可注入假模型）让测试不用真花钱调 LLM。
- **fail-open 的兜底思维**：检索门、图工作流，任何环节出错都「宁可退回安全路径」，而不是崩掉。
- **修 bug 加测试的闭环**：不只是在「改代码」，而是在「建立让 bug 无法复发的机制」。

### 6.3 认知层面（拔高）

- **按规模选技术**：单用户的事实量级，用 SQLite FTS5 就够，不必上向量库；但项目预留了 pgvector 升级路径（`PIKAKA_SEMANTIC_STORE=supabase`），接口不变。这是「不过度设计，但留好升级口」的判断力。

---

## 7. 可以改进的地方（体现批判性思维，一定要准备）

面试问「有什么不足/能改进什么」时，不要只说「挺好的」，要能说出**具体、可操作**的改进点。这里给你几个真实的方向：

### 7.1 安全与隔离（最值得讲）

- **`run_command` 工具还是骨架（skeleton）**：README 明确写了「需要一个真正的沙箱 + 安全边界」。如果 Agent 要执行终端命令，必须做沙箱隔离（容器/权限限制），否则有安全风险。这是我从「玩具」走向「生产」的最大鸿沟。

### 7.2 记忆的检索能力

- **目前是关键词检索（FTS5/BM25）**，对「语义相似」的查询会漏。比如用户说「我老板叫啥」而记忆里存的是「我的上司是张三」，关键词就搜不到。升级方向是换成向量语义检索（pgvector），或做混合检索（关键词 + 向量）。

### 7.3 记忆的准确性

- **整合（consolidation）可能提炼出错误的事实**，而且错误会一直留在库里。可以加一个「记忆校验」环节，或者让事实带置信度、过期时间。

### 7.4 中文支持

- FTS5 默认分词器对中文是「前缀匹配」的 hack（不切词），不是真正的中文分词。更好的是引入专门的中文分词器或向量检索。

### 7.5 评测的覆盖率

- 确定性测试覆盖了模块级逻辑，但**端到端的「行为测试」相对少**。可以加更多「给定用户输入 → 断言数据库最终状态」的集成测试。

### 7.6 生产化运维

- 目前是单机单进程，SQLite 单写者。多用户、并发场景需要换数据库、加队列、做会话隔离。但这是教学项目的定位决定的，不是缺陷。

> **讲改进点的技巧**：每讲一个不足，都补一句「我知道该怎么改」——比如「改成向量检索，项目里其实已经预留了 supabase 接口，换一个 env 变量就能切」。这样从「挑毛病」变成「有思路」。

---

## 8. 面试会问的问题 + 参考答案

### 8.1 高频必问（先背这四个）

**Q1：这个项目是做什么的？**
> 一个本地优先的个人助手 Agent，教学导向。用不到 100 行纯 Python、零框架，实现 Agent 的四大支柱：网关、循环、记忆、评测。我通过它搞懂了 ChatGPT 这类产品底层在干什么。

**Q2：Agent 的循环是怎么实现的？**
> 一个 `while` 循环：思考（调 LLM）→ 行动（执行工具）→ 观察（结果喂回）。两个退出条件：①模型不再要工具（自然结束）；②达到 `max_iterations`（硬停，防死循环）。和 LangGraph 的区别是：这是无框架的最简形态，LangGraph 是显式的图编排。

**Q3：记忆是怎么做的？**
> 分三类：语义记忆（事实，facts 表）、情景记忆（带日期的事件，episodes 表）、程序记忆（怎么做，SKILL.md）。存 SQLite，用 FTS5 做关键词检索。查的时候先过一个「检索门」——用小模型判断这轮要不要查记忆，避免每轮都查（慢 + 无关记忆带偏答案）。

**Q4：怎么评测它好不好用？**
> 分两套：确定性（pytest，判断「做对了没」，0/1）和 LLM 裁判（DeepEval，判断「好不好」，打分）。两者严格分开，`make gate` 一起跑作为发布门。

### 8.2 技术深度题（可能追问）

**Q5：检索门（retrieval gate）解决什么问题？为什么不用向量库？**
> 解决两个问题：①每轮都查记忆太慢；②无关记忆会带偏答案。方案是先用便宜小模型判断「要不要查」，失败时 fail-open（宁可查）。向量库：单用户事实量级下关键词检索（BM25）够用、全本地、索引可读；想升级换 `PIKAKA_SEMANTIC_STORE=supabase` 切 pgvector，接口不变。

**Q6：什么是依赖注入？你项目里怎么用的？**
> 把依赖（如模型客户端、数据库连接）作为参数传进构造函数，而不是在类内部硬编码。好处是测试时能注入「假模型」（不用真花钱调 LLM），dashboard 能注入「跨线程连接」。`Pikaka(client=..., conn=...)` 就是这两个缝。

**Q7：多模型怎么适配的？**
> 不同供应商接口「方言」不同，主要分 Anthropic 和 OpenAI 两种格式。用一个 `PROVIDERS` 字典登记 11 家，再用 ~60 行的 `OpenAICompatClient` 把 OpenAI 格式翻译成 Anthropic 格式。循环代码只认一种格式，换供应商不用改循环。

**Q8：工作记忆和长期记忆的区别？**
> 工作记忆（session.py 拼的系统提示词）是「用一次就扔」的：人设 + 当前时间 + 相关记忆 + 最近 N 轮历史，每轮重新拼。长期记忆存在 SQLite 里，通过检索门按需捞出来。用滑动窗口（最近 12 轮）保证上下文/成本不随对话变长而无限膨胀。

### 8.3 行为题（项目相关）

**Q9：你在项目里遇到的最难的问题是什么？**
> （讲第 3.2 节的中文路径崩溃）——体现定位底层 bug 的能力。

**Q10：你怎么保证这个项目不是「跑起来就完事」，而是真的可靠？**
> 讲 5.5 的验证过程 + 5.4 的可观测性（trace 常开、花费记账、dashboard 实时看）。

**Q11：你从中学到了什么？（开放题）**
> 讲第 6 节，分技术/工程/认知三层。

### 8.4 诚实作答的边界（很重要）

- **如实说这是「研读 + 部署 + 二次验证」的开源项目**，不是从零写的。这完全正当——实习生能读懂、能讲清一个生产级 Agent 的骨架，本身就是能力。
- **不要夸大**：不要说自己「开发了」这个项目，要说「部署并深入研读了」，然后重点讲**你理解了哪些设计、踩了什么坑、做了什么验证**。
- **能讲出「为什么这么设计」比「写了多少代码」更值钱**。面试官考的是理解，不是行数。

---

## 9. 简历怎么写（可直接粘贴）

### 9.1 项目栏（中文版，可直接用）

**pikaka-agent：本地优先个人助手 Agent**（Python / SQLite / Anthropic API / uv）

- 部署并深入研读了一个**零框架、教学导向**的 Agent 系统，核心循环仅 ~95 行纯 Python，完整覆盖「网关 → 工作记忆 → 循环 → 记忆 → 评测」链路
- 理解并验证了 **Retrieval Gate**（低成本小模型 judge 判断是否检索记忆，fail-open 兜底），在保证准确性的同时降低 token 成本与无关记忆带来的答案偏置
- 剖析 **SQLite + FTS5** 构建的三支柱记忆系统（语义/情景/程序），并定位 FTS5 对中日韩文字不切词的问题
- 通过 ~60 行适配器统一接入 11 个 LLM 供应商（Anthropic / OpenAI 两种协议），做到「换供应商不改循环」
- 建立**确定性评测（pytest）与 LLM 裁判评测（DeepEval）分离**的评测体系，并跑通发布门 `make gate`
- 实现全链路可观测：JSONL trace 常开 + token 花费只增账本 + Web dashboard 可视化（stdlib http.server + 纯静态前端）

### 9.2 技能栏（按需勾选）

`Python` / `SQLite` / `pytest` / `uv` / `Anthropic & OpenAI API` / `Agent 架构` / `RAG / 记忆系统` / `评测（Eval）`

### 9.3 一句话亮点（放项目描述开头，抓眼球）

> 「用不到 100 行 Python 读懂了一个 Agent 的完整骨架——这是我看完 ChatGPT 底层原理后做的验证。」

---

## 附：自我介绍模板

**30 秒**：第 1 节那段。

**1 分钟**（第 1 节 + 一句技术亮点）：
> 「我部署并研读了一个本地优先的个人助手 Agent 项目 pikaka-agent。它不用任何框架，用不到 100 行 Python 把 Agent 的四大支柱——网关、循环、记忆、评测——都实现了。我最受益的两点：一是它的**检索门**设计，用一个小模型判断「这轮要不要查记忆」，既省钱又避免无关记忆带偏答案；二是它的**评测体系**，把「做对了没」（pytest）和「好不好」（LLM 打分）严格分开。我还踩了一个 Windows 中文路径导致 Python 崩溃的坑，一路定位到了 `.pth` 文件的编码问题。」

**3 分钟**：完整讲第 2 节架构 → 第 3 节搭建和踩坑 → 第 5 节评测 → 第 6 节收获。

---

> **最后叮嘱**：这份文档里的每个技术点我都对着源码核对过，放心讲。面试时最打动人的不是「我用了 React/SQLite」，而是「我能说清楚 Agent 循环为什么这么写、检索门为什么必要、评测为什么要分两套」。把「为什么」讲清楚，就赢了。
