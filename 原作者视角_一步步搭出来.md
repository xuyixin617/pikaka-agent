# 假如我是原作者：确定性构建流程 + 每个文件的精确内容

> 这份文档只给**确定的、能查证的答案**：每个阶段我建了哪个文件、里面有哪些**类/函数/表/字段**、默认值是多少、流程是哪几步。
> 不解释代码语法，只给你「精确的事实」。你拿着这份对照源码，每个字都能对上。

---

## 构建总路线（11 个阶段，顺序是固定的）

```
① config.py        → 定义配置（27 个字段的 Settings 类）
② loop/models.py   → 接入 11 家模型（PROVIDERS 字典）
③ db.py            → 定义数据库（4 张表 + 5 个触发器）
④ loop/agent.py    → 写核心循环（run_loop 函数）
⑤ tools/           → 写工具系统（ToolRegistry + 8 个工具）
⑥ memory/          → 搭记忆（Memory 门面 + 3 类 store + 检索门 + 整合）
⑦ runtime/session.py → 拼工作记忆（Session 类）
⑧ app.py           → 装配（Pikaka 类）
⑨ gateway/         → 开入口（7 个网关文件）
⑩ ops/ + evals/    → 可观测 + 评测
⑪ graph/ skills/ docs/ examples/ → 补充扩展
```

**顺序为什么固定**：每阶段依赖前一阶段的产物。④ 的循环要调 ② 的模型；⑥ 的记忆要读写 ③ 的表；⑧ 的装配要把 ①②③⑤⑥⑦ 全连起来。

---

## 阶段 ①：配置中心 `pikaka/config.py`

**产出物**：1 个 `Settings` 数据类（27 个字段）+ 2 个函数（`load_settings()`、`ensure_home()`）。

**确定性答案 —— Settings 的字段（环境变量 → 默认值 → 干什么）**：

| 字段 | 环境变量 | 默认值 | 干什么 |
|---|---|---|---|
| provider | PIKAKA_PROVIDER | anthropic | 用哪家模型 |
| api_key | PIKAKA_API_KEY | 空 | 密钥（可覆盖） |
| base_url | PIKAKA_BASE_URL | None | 接口地址（可覆盖） |
| model | PIKAKA_MODEL | 空 | 主模型名（空则用供应商默认） |
| small_model | PIKAKA_SMALL_MODEL | 空 | 小模型名（检索门/整合用） |
| home | PIKAKA_HOME | `.pikaka` | 数据存哪 |
| max_iterations | PIKAKA_MAX_ITERATIONS | **10** | 循环上限 |
| max_tokens | PIKAKA_MAX_TOKENS | **8192** | 单次输出上限 |
| history_turns | PIKAKA_HISTORY_TURNS | **12** | 工作记忆留几轮 |
| consolidate_every | PIKAKA_CONSOLIDATE_EVERY | **6** | 每几轮整合一次记忆 |
| retrieval_top_k | PIKAKA_RETRIEVAL_TOP_K | **4** | 检索几条事实 |
| semantic_store | PIKAKA_SEMANTIC_STORE | sqlite | 语义记忆后端 |
| episodic_store | PIKAKA_EPISODIC_STORE | sqlite | 情景记忆后端 |
| apple_calendar / google_calendar / google_calendar_id / apple_tools | — | 关 | 日历同步开关 |
| gh_tool / gh_repo | PIKAKA_GH_TOOL / PIKAKA_GH_REPO | 关/空 | GitHub 只读工具 |
| experimental | PIKAKA_EXPERIMENTAL | 关 | 实验性工具（子代理） |
| graph_workflows | PIKAKA_GRAPH_WORKFLOWS | 关 | 图工作流开关 |
| telegram_token / whatsapp_token / whatsapp_phone_number_id | — | 空 | 各网关的令牌 |
| otel_endpoint | OTEL_EXPORTER_OTLP_ENDPOINT | 空 | 深度追踪端点 |

**确定性流程**：`load_settings()` → 读 `.env` → 返回 `Settings()` 实例。每个字段用一个 `default_factory` 从环境变量取值，取不到就用默认值。

**为什么第一步**：后面每个模块都要 `from pikaka.config import Settings`。不先定好，后面到处是写死的魔法数字。

---

## 阶段 ②：模型接入 `pikaka/loop/models.py`

**产出物**：`PROVIDERS` 字典（**11 家**）、`Provider` 数据类、`get_client()` 函数、`OpenAICompatClient` 类（~60 行适配器）。

**确定性答案 —— 11 家供应商清单（名字 → 协议 → 密钥环境变量 → 默认模型）**：

| provider | 协议（wire format） | 密钥变量 | 主模型 / 小模型 |
|---|---|---|---|
| anthropic | anthropic（原生） | ANTHROPIC_API_KEY | claude-sonnet-5 / claude-haiku-4-5-20251001 |
| openai | openai（适配） | OPENAI_API_KEY | gpt-5.5 / gpt-4.1-mini |
| openrouter | openai（适配） | OPENROUTER_API_KEY | （免费模型） |
| gemini | openai（适配） | GEMINI_API_KEY | gemini-3.5-flash |
| deepseek | openai（适配） | DEEPSEEK_API_KEY | deepseek-v4-pro |
| minimax | anthropic（原生） | MINIMAX_API_KEY | MiniMax-M3 / MiniMax-M2 |
| kimi | anthropic（原生） | MOONSHOT_API_KEY | kimi-k3 / kimi-k2.6 |
| **glm** | anthropic（原生） | ZHIPU_API_KEY | glm-5.2 / glm-5-turbo |
| xai | openai（适配） | XAI_API_KEY | grok-4 |
| opencode_zen | openai（适配） | OPENCODE_ZEN_API_KEY | deepseek-v4-flash-free |
| opencode_go | openai（适配） | OPENCODE_GO_API_KEY | deepseek-v4-flash |

**确定性流程**：`get_client()` 做 4 件事：
1. 按 `settings.provider` 从 PROVIDERS 找到对应 Provider
2. 读密钥（`settings.api_key` 优先，否则读该 provider 的 `key_env` 环境变量）
3. 没密钥 → 打印「去哪申请密钥」的提示并退出
4. 补全默认模型名（`settings.model` 空则用 provider.model）
5. 按协议返回：anthropic 原生 → `anthropic.Anthropic()`；openai → `OpenAICompatClient()`

**关键设计**：上层循环只认 Anthropic 的一种调用格式。OpenAI 系的 7 家，全靠 `OpenAICompatClient` 这个 ~60 行适配器翻译。

---

## 阶段 ③：数据库 `pikaka/db.py`

**产出物**：`SCHEMA`（一段 SQL，建 4 张表 + 5 个触发器）、`connect()` 函数、`_migrate()` 函数。

**确定性答案 —— 4 张表及其字段（精确到列）**：

**表 1：`calendar_events`（日程）**
| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER 主键 | |
| title | TEXT | 标题 |
| start | TEXT | 开始时间（ISO 8601） |
| end | TEXT | 结束时间 |
| attendees | TEXT | 参与者（逗号分隔） |
| notes | TEXT | 备注 |
| created_at | TEXT | 创建时间 |

**表 2：`facts`（事实记忆）+ 索引**
| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER 主键 | |
| subject | TEXT | 关于谁（如 'alex'） |
| content | TEXT | 事实内容 |
| source | TEXT | 来源：'user'（你直接说）或 'consolidation'（整合提炼） |
| created_at | TEXT | 创建时间 |

配套：`facts_fts`（FTS5 全文索引）+ **3 个触发器** `facts_ai`/`facts_ad`/`facts_au`（插入/删除/更新时自动同步索引）。

**表 3：`episodes`（情景记忆）+ 索引**
| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER 主键 | |
| happened_at | TEXT | 事件发生的日期（ISO 8601） |
| summary | TEXT | 事件摘要 |
| created_at | TEXT | 创建时间 |

配套：`episodes_fts` + 2 个触发器 `episodes_ai`/`episodes_ad`。

**表 4：`chat_log`（聊天记录）**
| 列 | 类型 | 说明 |
|---|---|---|
| id | INTEGER 主键 | |
| role | TEXT | 'user' 或 'assistant' |
| content | TEXT | 内容 |
| consolidated | INTEGER | 是否已整合（0/1） |
| session_id | TEXT | 属于哪次会话 |
| created_at | TEXT | 创建时间 |

（迁移 `_migrate` 会补加 2 列：`source` 消息来源、`meta` 每轮遥测 JSON）

**确定性流程**：`connect(home)` → 打开 `home/state.db` → 执行 `SCHEMA` 建表 → 跑 `_migrate` 补列 → 返回连接。

**关键答案**：数据库就是一个文件 `state.db`，用 SQLite，不用服务器。`FTS5` 是让 `facts`/`episodes` 能被「关键词搜索」的全文索引。

---

## 阶段 ④：核心循环 `pikaka/loop/agent.py`

**产出物**：`run_loop()` 函数、`LoopResult` 数据类（3 个字段）、`Observer` 类型别名。

**确定性答案 —— run_loop 的精确流程（编号）**：

```
输入：client, model, system(提示词), messages(历史), tools, max_iterations, max_tokens

for iteration in 1 .. max_iterations:        # 最多跑 max_iterations 轮
    ① reason  调 LLM：client.messages.create(model, system, messages, tools)
    ②         把 assistant 的回复 append 进 messages
    ③         提取 tool_use（模型请求调用的工具）
    ④ 护栏1  如果 tool_use 为空 → 拼接文本块，return LoopResult（自然结束）
    ⑤ act     逐个执行 tools.execute(工具名, 参数)
    ⑥ observe 把每个工具结果 append 进 messages（role=user）
    回到循环开头（再 reason）

⑦ 护栏2  循环跑满 max_iterations → return 固定提示文本（硬停，防死循环）
```

**两个退出条件（确定的）**：
1. 护栏 1：模型不再请求工具 → 自然结束。
2. 护栏 2：达到 `max_iterations`（默认 10）→ 硬停，返回「我达到迭代上限了」。

**LoopResult 的 3 个字段**：`reply`（最终回复文本）、`tool_calls`（这轮调过哪些工具）、`iterations`（循环跑了几轮）。

---

## 阶段 ⑤：工具系统 `pikaka/tools/`

**产出物**：`registry.py`（`Tool` 数据类 + `ToolRegistry` 类）+ 各工具文件 + `tools/__init__.py` 的 `build_registry()`。

**确定性答案 —— Tool 的 4 个字段**：`name`（名字）、`description`（给模型看的说明）、`input_schema`（参数定义）、`fn`（干活的函数）。

**确定性答案 —— 默认注册的 8 个工具（名字 → 干什么）**：

| 工具名 | 来自文件 | 干什么 |
|---|---|---|
| create_event | calendar.py | 建日程 |
| list_events | calendar.py | 查日程 |
| save_note | notes.py | 记笔记 |
| send_message | messages.py | 发消息 |
| search_web | search.py | 搜网页 |
| manage_memory | memory_admin.py | 改/删记忆 |
| update_soul | memory_admin.py | 更新人设 |
| create_skill | memory_admin.py | 创建技能 |

**可选工具（开关控制）**：`delegate_task`（experimental=1 时，派活给 pi 子代理）、Apple 生态工具（apple_tools=1）、GitHub 只读工具（gh_tool=1）、MCP 服务器工具（存在 .pikaka/mcp.json）。

**确定性流程 —— ToolRegistry.execute() 的 3 步**：
1. 按名字找工具，找不到 → 返回 `"Error: unknown tool 'xxx'"`
2. 找到 → 执行 `fn(**args)`
3. 执行出错 → 返回 `"Error running xxx: ..."`（**返回错误文本，不让程序崩溃**）

---

## 阶段 ⑥：记忆系统 `pikaka/memory/`

**产出物**：`memory/__init__.py`（`Memory` 门面类）、`retrieval_gate.py`、`consolidation.py`、`semantic/`、`episodic/`、`procedural/`。

**确定性答案 —— Memory 类持有 3 个 store**：
- `self.facts`：语义记忆，按 `semantic_store` 决定用哪个实现，共 **5 种可选**：`SqliteFactStore`（默认）/ `SupabaseFactStore` / `Mem0FactStore` / `ZepFactStore` / `LangMemFactStore`
- `self.episodes`：情景记忆，**2 种可选**：`SqliteEpisodeStore`（默认）/ `NotionEpisodeStore`
- `self.skills`：程序记忆，`SkillLoader`

**确定性答案 —— Memory 的 7 个方法**：
| 方法 | 干什么 |
|---|---|
| gated_retrieve(message) | 先过检索门，需要才检索 facts(top_k=4) + episodes(top_k=3) |
| matching_skills(message) | 关键词匹配技能 |
| log_chat(...) | 写 chat_log（user + assistant 两行） |
| session_history(id) | 返回某次会话的 (user, assistant) 对话对 |
| list_sessions() | 列出所有会话 |
| export_markdown() | 生成人类可读的 MEMORY.md |
| maybe_consolidate() | 必要时触发整合 |

**检索门 `retrieval_gate.py` 的确定性流程**：
1. 用 `small_model`（小模型）问一句「这条消息需要查记忆吗」
2. 返回 JSON：`{retrieve: true/false, query: 关键词, reason: 原因}`
3. **fail-open**：解析失败/出错 → 返回 `retrieve=True`（宁可查，不漏记忆）

**整合 `consolidation.py` 的确定性流程**：
1. 查 chat_log 里 `consolidated=0`（未整合）的行数
2. 攒够 `consolidate_every`（默认 6）× 2 行 → 才整合
3. 用小模型把聊天提炼成 `{facts: [...], episode: "..."}`
4. 写进 facts / episodes 表，把 source 标为 'consolidation'
5. 把这几行 chat_log 的 `consolidated` 标成 1

---

## 阶段 ⑦：工作记忆 `pikaka/runtime/session.py`

**产出物**：`Session` 类、`load_soul()` 函数、`DEFAULT_SOUL` 常量。

**确定性答案 —— build_system() 拼提示词的 5 块内容（按顺序）**：
1. **人设**：`load_soul()` 读 `SOUL.md`（不存在则用 `DEFAULT_SOUL` 创建）
2. **当前时间**：`Right now it is {星期, 年月日 时:分} (时区)`
3. **模型自述**：`Your model: you are running on '{model}' via the '{provider}' provider...`
4. **相关记忆**：`memory.gated_retrieve(消息)` 的结果（可能为空）
5. **相关技能**：`memory.matching_skills(消息)` 的结果（可能为空）

**确定性答案 —— Session 的其他方法**：`add_exchange()`（记一轮对话 + 写 chat_log）、`start_new()`（清空历史开新会话）、`switch(id)`（切换历史会话）。

---

## 阶段 ⑧：装配 `pikaka/app.py`

**产出物**：`Pikaka` 类（`respond()`、`_run_full_turn()`、`_respond_via_graph()`、`close()` 方法）。

**确定性答案 —— Pikaka 初始化的装配顺序（7 步）**：
```
1. self.settings = 读配置
2. self.conn = 连数据库 connect(home)
3. self.client = 连模型 get_client(settings)
4. self.memory = Memory(conn, settings, client)   # 记忆最先建，因为工具要用
5. self.tools = build_registry(conn, settings, memory)
6. self.session = Session(settings, memory)
7. self.tracer = Tracer(settings)
```

**确定性答案 —— respond() 一次对话的流程（编号）**：
```
1. build_system(消息) → 拼提示词
2. run_loop(...) → 跑循环（阶段④）
3. session.add_exchange(...) → 存这轮对话
4. memory.maybe_consolidate() → 必要时整合
5. memory.export_markdown() → 刷新 MEMORY.md
6. tracer.end_turn(...) → 结束记录
7. return LoopResult
```

**确定性答案 —— 依赖注入**：`Pikaka(settings=None, client=None, conn=None)` 三个参数都可传。测试时传「假模型」，网页版传「跨线程连接」。

---

## 阶段 ⑨：网关 `pikaka/gateway/`

**产出物 —— 7 个文件（确定性清单）**：
| 文件 | 入口 |
|---|---|
| cli.py | 终端聊天（`pikaka` 默认） |
| runner.py | 把同步核心包成异步（供 Telegram 等用） |
| supervisor.py | 多进程监督 |
| telegram.py | Telegram 机器人 |
| discord.py | Discord 机器人 |
| whatsapp.py | WhatsApp |
| voice.py | 语音 |

**确定性答案**：`pikaka/__main__.py` 的入口分发——`pikaka`（默认→cli）、`pikaka dashboard`、`pikaka voice`、`pikaka telegram`、`pikaka discord`、`pikaka whatsapp`、`pikaka brief`、`pikaka gather`、`pikaka skill install`。

---

## 阶段 ⑩：可观测 `pikaka/ops/` + 评测 `evals/`

**ops/ 的确定性清单（文件 → 干什么）**：
| 文件 | 干什么 |
|---|---|
| tracing.py | 每次对话写 trace（永远开） |
| pricing.py | 算 token → 钱 |
| dashboard.py | 本地网页（127.0.0.1:7777） |
| scoring.py / judge.py | 打分 / LLM 裁判 |
| release_gate.py | 发布门（评测全过才放行） |
| brief.py / gather.py | 简报 / 汇总 |
| arena.py / memory_arena.py | 记忆后端对比测试 |
| static/ | 前端静态文件（HTML/CSS/JS） |

**evals/ 的确定性清单**：
- `deterministic/`：**60+ 个** pytest 测试文件（判断「做对了没」，0/1）
- `judge/`：3 个文件（`test_response_quality.py`、`test_retrieval_gate_accuracy.py`、`anthropic_judge.py`），LLM 打分
- `conftest.py`、`helpers.py`、`dataset.jsonl` 等

**确定性答案 —— 3 个命令**：`make eval`（确定性）、`make eval-judge`（裁判）、`make gate`（两者都要过）。

---

## 阶段 ⑪：补充扩展

- `graph/`：`engine.py`（图引擎）+ `nodes.py` + `workflows/triage.py`、`workflows/gather.py`（图工作流，开关控制）
- `skills/`：`TEMPLATE.md` + `schedule-meeting/` + `weekly-brief/` + `community/`
- `docs/`：架构文档 + 可编辑白板图
- `examples/`：`tiny_memory_agent.py`（最小记忆 agent 示例）等

---

## 附录：总速查表（文件 → 精确的类/函数/表）

| 文件 | 里面到底有什么（精确） |
|---|---|
| config.py | `Settings` 类（27 字段）+ `load_settings()` + `ensure_home()` |
| loop/models.py | `PROVIDERS`（11 家）+ `Provider` + `get_client()` + `OpenAICompatClient` |
| db.py | `SCHEMA`（4 表 + 5 触发器）+ `connect()` + `_migrate()` |
| loop/agent.py | `run_loop()` + `LoopResult`(reply/tool_calls/iterations) |
| tools/registry.py | `Tool`(name/description/input_schema/fn) + `ToolRegistry` |
| tools/__init__.py | `build_registry()` → 注册 8 个默认工具 |
| memory/__init__.py | `Memory` 类（7 方法）+ 3 个 store |
| memory/retrieval_gate.py | `should_retrieve()` → 返回 (retrieve, query, reason) |
| memory/consolidation.py | `consolidate_if_due()` |
| runtime/session.py | `Session` + `build_system()` + `load_soul()` + `DEFAULT_SOUL` |
| app.py | `Pikaka` 类（respond/_run_full_turn/_respond_via_graph/close） |

---

> **这份文档的正确用法**：拿它当「对照表」——读一个文件前，先看这张表「这文件里应该有几个类、几个表、几个字段」，然后打开源码核对，你会发现「果然全对得上」。这样你脑子里装的就是「确定的事实」，而不是「大概的印象」。
