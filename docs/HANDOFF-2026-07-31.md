# pikaka-agent — handoff, 2026-07-31

Paste this whole file as the first message of the next session.

---

## Your job

Two deliverables for Sean's YouTube system-design video, filming imminent:

1. **`docs/loop-vs-graph.md`** — loop vs graph engineering, explained clearly, with
   concrete use cases that can actually be built and tested in THIS repo.
2. **An Excalidraw chart** of the same, in Sean's hand-drawn style. Use the
   `excalidraw` skill (`.claude/skills/excalidraw/`) — it encodes his visual
   language. Existing boards to match: `docs/whiteboards/k3-architecture.excalidraw`,
   `pi-architecture.excalidraw`.

Do NOT re-derive the conclusions below. They were reached by measuring.

---

## The state of the repo

- Branch `main` @ `7dbe6bf`, clean, in sync. **291 offline deterministic tests, ruff clean.**
- Run tests: `PIKAKA_PROVIDER=anthropic ANTHROPIC_API_KEY= .venv/bin/python -m pytest evals/deterministic -q`
  (blanking the key runs the offline tier; with a key, live model tests run and one is flaky).
- `main` is **protected**: `git push origin main` is REJECTED for everyone.
  Ship via `git checkout -b <topic>` → `gh pr create --fill` → `gh pr checks --watch` → `gh pr merge --squash --delete-branch`.
- Open PRs: **#42** @Tian-Timm (bundled OAuth client, new), **#36** @AllenHK (whiteboard, awaiting his layout pass).
- 644 stars. Contributors: @Tian-Timm 6 merged, @wilenWang 3, @epicnellson 1.

## The graph work — parked, not merged, and that is deliberate

Branch `claude/graph-engineering-agent-graphs-fe54kz` @ `fe99dd3`, **4 ahead / 4 behind main**.
A 433-line stdlib graph engine (`pikaka/graph/`) plus one workflow (`triage`) plus a
dashboard Graph tab. 311 tests on that branch. Design doc: `docs/agent-graphs-design.md`.

**It works.** Verified live in both the CLI and dashboard: "thanks!" routes quick
(haiku, ~2.0s); "what is on my calendar" routes full (opus, ~10s, gate fires,
tool runs). Trace events land in the same JSONL.

**It should not be merged yet, for reasons Sean agreed with:**

- The shipped workflow (`triage`) is a **weak example of a graph**. Its two "parallel"
  branches are one LLM call and a local `.ics` file read that takes ~40ms, and
  **the router never reads the calendar value it fetched**. The real win — cheap
  model answers small talk — is one `if`, not 433 lines.
- Sean opened the Graph tab and said *"i dont understand what's going on here"*.
  He owns the repo. That is the review.
- It contradicts three rules he wrote into `CONTRIBUTING.md` days earlier: the
  footprint ladder (a new subsystem is rung 6, last resort), "speculative
  infrastructure with no second caller yet" (on the declined-even-when-well-built
  list), and the four-pillars legibility bar.
- Per the design doc §11, AutoManus will **copy** the engine, not import it. So
  merging into pikaka unblocks nobody.

**Do not relitigate this in the video's favour.** The honest story is more
interesting than a launch.

---

## The conclusions that matter, all measured

**The one-line distinction Sean landed on and likes:**
> A loop **discovers** what to do next. A graph **pre-determines** what happens next.

**Loop wins** when you cannot draw the steps in advance — "debug why my test is
failing", where every step depends on what the last one found. That is most of
what pikaka does.

**Graph wins** when you already know the shape and steps can overlap.

**Sean's own realisation, and the strongest hook in the material:**
> He opened pikaka's existing architecture diagram — gateway → memory → loop →
> tools → reply, fixed order every turn — and said *"that's a graph. i just had it
> hardcoded in python instead of nodes and edges."*
>
> So a graph engine does not give you the shape. You already had it. It makes the
> shape **data instead of code**: inspectable, drawable from the code itself,
> partly parallel, re-routable without rewriting the function.

**The skeptical framing he wants to lead with** (his instinct, and correct):
> "Isn't this just 'deterministic workflow' vs 'let the agent decide' from 2023?"
> Mostly yes. Airflow/Temporal/Step Functions have done conditional parallel DAGs
> for a decade. What is genuinely new is narrow: **(1) the nodes are
> non-deterministic, (2) a model can pick the edge, (3) so you need guardrails
> Airflow never needed** — pikaka's engine caps visits per node and total steps.
> Old shape, new contents.

**Context:** Peter Steinberger (@steipete) posted twelve words on 2026-07-18 —
*"Are we still talking loops or did we shift to graphs yet?"* — 2.9M views. He did
not define anything; he poked the field. Andrew Ng's graph course reignited it.
The emerging consensus — *graphs extend loops, the loop becomes one node* — is
exactly what pikaka's `full_agent` node does.

---

## The use cases to document (ranked by how well they'd demo)

**1. Multi-calendar read — the BEST one, and it is real.**
Sean has Apple + Google + pikaka's local calendar. Loop: model calls one tool, reads
the result, decides to call another, three sequential round trips. Graph: fan out
to all three at once, merge, answer.
- Google API measured at **0.39s**; AppleScript at **~12s**. Real parallelism, real numbers.
- Half of it already exists: `pikaka/tools/calendar.py::make_list_tool` now merges
  Google + local (shipped in #40).
- Building this as a graph workflow fixes a real problem AND makes the demo obvious.

**2. Morning brief — three genuinely different branches.**
calendar ∥ memory search (FTS5) ∥ outbox → synthesize → router: anything urgent?
`pikaka/ops/brief.py` already exists as a starting point.

**3. Fact-check my memory — the only way to exercise bounded cycles.**
pick a fact → search web → judge → (outdated → update → re-judge, `max_visits=2`).
Sean's memory has ~36 facts and some are stale, so it does something real.

**Weak examples, say so honestly:** triage (shipped, but its parallelism saves
40ms); the content workflow from design doc §4.2 (blocked — it needs `human_node`,
which does not exist in `pikaka/graph/nodes.py`).

---

## What is missing or could be improved

**1. Tool test coverage is the biggest hole.** 7 of 13 registered tools have ZERO
test references: `browse_web`, `create_note`, `create_reminder`, `list_events`,
`read_apple_mail`, `schedule_task`, `send_message`. `list_events` is the tool that
told Sean his calendar was empty while his real Thursday was full.

**2. The eval suite cannot see reality.** It tests string generation, never whether
a call succeeds. That is why every Apple tool was broken for weeks while CI stayed
green. The old test literally said *"covered by ... manual verification on the dev
machine"*. `evals/deterministic/test_apple_tools.py` (new) pins what CAN be checked
offline; the structural gap remains.

**3. `pikaka/ops/dashboard.py` is still the largest file** (~893 lines after the split).

**4. Apple Mail is genuinely unusable** — Mail.app cannot answer
`count of messages of inbox` inside two minutes (`-1712`). Documented, fails fast
in 20s with an honest message. Not our bug.

**5. `PIKAKA_APPLE_TOOLS=1` costs ~13s per calendar question** for one line Google
already has. Consider turning it off in `.env`, or folding Apple into `list_events`
behind a local-calendars-only filter.

**6. Google Calendar has no shipped OAuth client yet** — `CLIENT_ID`/`CLIENT_SECRET`
in `pikaka/tools/google_calendar.py` are empty stubs. PR #42 addresses this.
Decision already taken: **readonly scope** for the shipped client; write access
stays behind the user's own `.pikaka/credentials.json`.

**7. `.venv` was destroyed** by a stray `ln -sfn` during worktree cleanup and had to
be rebuilt. If anything imports oddly, rebuild:
`uv venv --python 3.11 && VIRTUAL_ENV=$PWD/.venv uv pip install -e '.[dev,eval,gcal]'`

---

## Conventions that will bite you

- **Never merge, approve or close a PR without Sean's explicit yes for THAT PR.**
  "sure", "go", "continue" on a broader task is NOT approval. See
  `.claude/skills/review-pr/SKILL.md`. This was violated once and he was not happy.
- **Never run `scripts/demo_seed.py`** or anything that clears `.pikaka` without
  asking immediately before each run. It wipes memory, calendar, traces, spend ledger.
- **No emojis** in any UI surface (dashboard, CLI, README prose).
- **Be concise.** He has said so repeatedly and will say it again. Lead with the
  answer. **Always end a turn with a short executive summary.**
- When drafting anything he'll publish as himself: lowercase, short lines, **no
  em-dashes**, no crafted punchlines. He raises the objection himself then answers
  it. See the `sean-writing-voice` memory.
- Test PRs in a **git worktree**, never `gh pr checkout` (his dashboard runs from
  the main tree). Do NOT symlink `.venv` into worktrees — that is what destroyed it.
- `.pikaka/credentials.json`, `.pikaka/google-token.json`, `.env` are all gitignored and
  pinned by `evals/deterministic/test_runtime_data_is_ignored.py`. Keep it that way.

---

## Suggested first move

Read `docs/agent-graphs-design.md` and `pikaka/graph/workflows/triage.py`, then write
`docs/loop-vs-graph.md`. Show Sean an outline before writing the whole thing — he
will want to shape the framing, and he prefers one item at a time.
